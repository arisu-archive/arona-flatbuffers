# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class KeyMappingPopupNoneFocusExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = KeyMappingPopupNoneFocusExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsKeyMappingPopupNoneFocusExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # KeyMappingPopupNoneFocusExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # KeyMappingPopupNoneFocusExcel
    def PrefabName(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # KeyMappingPopupNoneFocusExcel
    def ButtonName(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def KeyMappingPopupNoneFocusExcelStart(builder):
    builder.StartObject(2)

def Start(builder):
    KeyMappingPopupNoneFocusExcelStart(builder)

def KeyMappingPopupNoneFocusExcelAddPrefabName(builder, prefabName):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(prefabName), 0)

def AddPrefabName(builder, prefabName):
    KeyMappingPopupNoneFocusExcelAddPrefabName(builder, prefabName)

def KeyMappingPopupNoneFocusExcelAddButtonName(builder, buttonName):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(buttonName), 0)

def AddButtonName(builder, buttonName):
    KeyMappingPopupNoneFocusExcelAddButtonName(builder, buttonName)

def KeyMappingPopupNoneFocusExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return KeyMappingPopupNoneFocusExcelEnd(builder)


class KeyMappingPopupNoneFocusExcelT(object):

    # KeyMappingPopupNoneFocusExcelT
    def __init__(
        self,
        prefabName = None,
        buttonName = None,
    ):
        self.prefabName = prefabName  # type: Optional[str]
        self.buttonName = buttonName  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        keyMappingPopupNoneFocusExcel = KeyMappingPopupNoneFocusExcel()
        keyMappingPopupNoneFocusExcel.Init(buf, pos)
        return cls.InitFromObj(keyMappingPopupNoneFocusExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, keyMappingPopupNoneFocusExcel):
        x = KeyMappingPopupNoneFocusExcelT()
        x._UnPack(keyMappingPopupNoneFocusExcel)
        return x

    # KeyMappingPopupNoneFocusExcelT
    def _UnPack(self, keyMappingPopupNoneFocusExcel):
        if keyMappingPopupNoneFocusExcel is None:
            return
        self.prefabName = keyMappingPopupNoneFocusExcel.PrefabName()
        self.buttonName = keyMappingPopupNoneFocusExcel.ButtonName()

    # KeyMappingPopupNoneFocusExcelT
    def Pack(self, builder):
        if self.prefabName is not None:
            prefabName = builder.CreateString(self.prefabName)
        if self.buttonName is not None:
            buttonName = builder.CreateString(self.buttonName)
        KeyMappingPopupNoneFocusExcelStart(builder)
        if self.prefabName is not None:
            KeyMappingPopupNoneFocusExcelAddPrefabName(builder, prefabName)
        if self.buttonName is not None:
            KeyMappingPopupNoneFocusExcelAddButtonName(builder, buttonName)
        keyMappingPopupNoneFocusExcel = KeyMappingPopupNoneFocusExcelEnd(builder)
        return keyMappingPopupNoneFocusExcel
