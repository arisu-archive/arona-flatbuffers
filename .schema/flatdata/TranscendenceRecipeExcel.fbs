include "CurrencyTypes.fbs";
include "ParcelType.fbs";

namespace FlatData;

table TranscendenceRecipeExcel {
    parcel_id: [long];
    parcel_amount: [int];
    cost_currency_amount: long;
    cost_currency_type: CurrencyTypes;
    parcel_type: [ParcelType];
    dev_name: string;
    id: long;
}