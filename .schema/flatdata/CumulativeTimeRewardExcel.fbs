include "ParcelType.fbs";

namespace FlatData;

table CumulativeTimeRewardExcel {
    end_date: string;
    id: long;
    start_date: string;
    description: string;
    time_condition: [long];
    reward_id: [long];
    reward_parcel_type: [ParcelType];
    reward_amount: [int];
}