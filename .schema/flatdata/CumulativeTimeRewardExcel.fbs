include "ParcelType.fbs";

namespace FlatData;

table CumulativeTimeRewardExcel {
    id: long;
    description: string;
    start_date: string;
    end_date: string;
    time_condition: [long];
    reward_parcel_type: [ParcelType];
    reward_id: [long];
    reward_amount: [int];
}