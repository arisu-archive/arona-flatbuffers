include "ParcelType.fbs";

namespace FlatData;

table CumulativeTimeRewardExcel {
    end_date: string;
    reward_amount: [int];
    id: long;
    reward_parcel_type: [ParcelType];
    time_condition: [long];
    reward_id: [long];
    start_date: string;
    description: string;
}