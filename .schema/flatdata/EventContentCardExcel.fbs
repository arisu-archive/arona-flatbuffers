include "ParcelType.fbs";

namespace FlatData;

table EventContentCardExcel {
    icon_path: string;
    reward_parcel_type: [ParcelType];
    back_icon_path: string;
    card_group_id: int;
    reward_parcel_id: [long];
    event_content_id: long;
    localize_etc_id: uint;
}