include "MailType.fbs";
include "ParcelType.fbs";

namespace FlatData;

table DefaultMailExcel {
    reward_parcel_id: [long];
    mail_send_period_from: string;
    reward_parcel_amount: [long];
    id: long;
    mail_type: MailType;
    mail_send_period_to: string;
    reward_parcel_type: [ParcelType];
    localize_code_id: uint;
}