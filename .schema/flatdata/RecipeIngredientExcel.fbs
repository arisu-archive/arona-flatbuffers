include "ParcelType.fbs";
include "RecipeType.fbs";

namespace FlatData;

table RecipeIngredientExcel {
    ingredient_parcel_type: [ParcelType];
    ingredient_id: [long];
    id: long;
    cost_amount: [long];
    cost_time_in_second: long;
    cost_id: [long];
    ingredient_amount: [long];
    recipe_type: RecipeType;
    cost_parcel_type: [ParcelType];
}