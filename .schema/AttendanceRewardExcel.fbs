include "ParcelType.fbs";

namespace FlatData;

table AttendanceRewardExcel {
    attendance_id: long;
    day: long;
    reward_amount: [long];
    reward_icon: string;
    reward_id: [long];
    reward_parcel_type: [ParcelType];
}