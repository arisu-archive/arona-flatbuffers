namespace MX.Data.Excel;

table CameraExcel {
    unique_id: long;
    left_margin: float;
    move_instantly: bool;
    rotation_x: float;
    move_instantly_rotation_save: bool;
    use_rail_point_compensation: bool;
    min_distance: float;
    max_distance: float;
    rotation_y: float;
    bottom_margin: float;
    ignore_enemies: bool;
}

root_type CameraExcel;