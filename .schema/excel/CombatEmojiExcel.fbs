include "EmojiEvent.fbs";

namespace MX.Data.Excel;

table CombatEmojiExcel {
    unique_id: long;
    emoji_reversal: bool;
    order_of_priority: int;
    show_emoji_delay: int;
    emoji_event: EmojiEvent;
    emoji_turn_on: bool;
    show_default_bg: bool;
    emoji_duration: bool;
}

root_type CombatEmojiExcel;